// FitMind Library — DEPRECATED (removed in ship phase)
// Module was unreachable to users; DualAIEngine relocated to src/engines/
import { Redirect } from 'expo-router';
export default function FitMindLibraryRedirect() {
  return <Redirect href="/dashboard" />;
}
