# Sentry properties — disable source map uploads until production
defaults.org=fitquest-x4
defaults.project=react-native
cli.executable=node_modules/@sentry/cli/bin/sentry-cli
# Auth token not set — uploads will be skipped
