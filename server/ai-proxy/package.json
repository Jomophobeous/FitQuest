{
  "name": "fitquest-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets": "echo 'Run: npx wrangler secret put GROQ_API_KEY && npx wrangler secret put GROK_API_KEY && npx wrangler secret put OPENROUTER_API_KEY && npx wrangler secret put PROXY_APP_KEY'"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
