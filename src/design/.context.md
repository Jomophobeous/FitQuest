# Design System Context
# Load this for ANY visual/styling work.

## Files
- src/design/theme-system.ts — CANONICAL: spacing[], typography, radius exports
- src/design/themes/themeConfigs.ts — 8 theme color palettes
- src/design/themes/themeEffects.ts — Glass-morphism presets, animation easing
- src/design/motion.ts — Motion constants (MOTION object)
- src/context/ThemeContext.tsx — useTheme() hook, ThemeMode type

## Spacing (HARD RULE — numeric keys only)
theme.spacing[1]=4  theme.spacing[2]=8   theme.spacing[3]=12
theme.spacing[4]=16 theme.spacing[5]=20  theme.spacing[6]=24
theme.spacing[8]=32 theme.spacing[10]=40
NEVER: theme.spacing.md, theme.spacing.lg, hardcoded px

## Colors (HARD RULE — theme.colors.* only)
theme.colors.background, surface, accent, onAccent, text, textSecondary,
textMuted, error, success, warning, border, divider, surfaceVariant,
purple, indigo, accentDark

## Glass-morphism
import { getThemeEffects } from 'design/themes/themeEffects'
Presets: glassCard, glassButton, glassModal, glassNavbar

## Animation easing
fast: 180ms out(cubic) | base: 250ms inOut(ease)
slow: 320ms in(cubic) | bounce: spring(damping=8, stiffness=200)

## Touch targets
Minimum 48px on all interactive elements.

## Typography
typography.sizes: h1, h2, h3, h4, body, bodySmall, label, caption, captionSm
